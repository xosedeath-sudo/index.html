<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Will You Be My Valentine? 💖</title>

<style>
    body {
        margin: 0;
        height: 100vh;
        background: linear-gradient(135deg, #ffd1dc, #ffe6f0, #fff0f5);
        background-size: 400% 400%;
        animation: bgMove 10s ease infinite;
        display: flex;
        justify-content: center;
        align-items: center;
        font-family: 'Segoe UI', sans-serif;
        overflow: hidden;
        text-align: center;
    }

    @keyframes bgMove {
        0% { background-position: 0% 50%; }
        50% { background-position: 100% 50%; }
        100% { background-position: 0% 50%; }
    }

    .box {
        background: white;
        padding: 35px;
        border-radius: 28px;
        width: 90%;
        max-width: 380px;
        box-shadow: 0 25px 50px rgba(0,0,0,0.25);
        animation: pop 0.8s ease;
        z-index: 2;
    }

    h1 {
        color: #ff4d6d;
        font-size: 28px;
        margin-bottom: 10px;
    }

    p {
        font-size: 17px;
        color: #555;
        margin-bottom: 15px;
    }

    .cute {
        font-size: 15px;
        color: #ff7a9e;
        margin-bottom: 20px;
    }

    button {
        padding: 12px 24px;
        margin: 8px;
        border: none;
        border-radius: 30px;
        font-size: 16px;
        cursor: pointer;
        transition: transform 0.2s;
    }

    .yes {
        background: #ff4d6d;
        color: white;
    }

    .no {
        background: #eee;
        position: absolute;
        color: #555;
    }

    .music {
        background: #ffe0ea;
        color: #ff4d6d;
        font-size: 14px;
    }

    button:hover {
        transform: scale(1.1);
    }

    @keyframes pop {
        from { transform: scale(0); }
        to { transform: scale(1); }
    }

    .heart {
        position: absolute;
        font-size: 20px;
        animation: float 6s linear infinite;
        opacity: 0.8;
    }

    @keyframes float {
        0% { transform: translateY(100vh); }
        100% { transform: translateY(-10vh); opacity: 0; }
    }
</style>
</head>

<body>

<div class="box">
    <h1>Hey you 💕</h1>

    <p>
        Just a small moment…<br>
        for a very special person 🥹
    </p>

    <p class="cute">
        You make ordinary days feel softer,<br>
        warmer, happier ✨
    </p>

    <h2 style="color:#ff4d6d;">Will you be my Valentine? 🌹</h2>

    <button class="music" onclick="playMusic()">▶ Play cute music 🎶</button><br>

    <button class="yes" onclick="yesClicked()">YES 💖</button>
    <button class="no" id="noBtn" onmouseover="playfulNo()">NO 😜</button>
</div>

<audio id="music" loop>
    <source src="https://cdn.pixabay.com/audio/2022/03/15/audio_9c3b1b09c7.mp3" type="audio/mpeg">
</audio>

<script>
let noCount = 0;
const noTexts = [
    "NO 😜",
    "Are you sure? 🥺",
    "Think again 👀",
    "That hurt a little 😢",
    "Okay now you're teasing 😤",
    "You can’t escape 😌"
];

function playMusic() {
    document.getElementById("music").play();
}

function playfulNo() {
    const btn = document.getElementById("noBtn");

    btn.innerText = noTexts[noCount % noTexts.length];
    noCount++;

    btn.style.left = Math.random() * (window.innerWidth - 120) + 'px';
    btn.style.top = Math.random() * (window.innerHeight - 60) + 'px';
}

function yesClicked() {
    document.body.innerHTML = `
    <div style="
        display:flex;
        justify-content:center;
        align-items:center;
        height:100vh;
        background:linear-gradient(135deg,#ffd1dc,#ffe6f0);
        font-family:'Segoe UI',sans-serif;
        color:white;
        text-align:center;
        padding:20px;
    ">
        <div>
            <h1 style="font-size:40px;">YAYYYY 🥹❤️</h1>
            <p style="font-size:22px;">
                You just made my heart do a happy dance 💃💖<br><br>
                Thank you for being you 🌸<br>
                Happy Valentine’s 💕
            </p>
        </div>
    </div>`;
}

setInterval(() => {
    const heart = document.createElement('div');
    heart.className = 'heart';
    heart.innerHTML = '💗';
    heart.style.left = Math.random() * 100 + 'vw';
    heart.style.fontSize = (Math.random() * 18 + 14) + 'px';
    document.body.appendChild(heart);
    setTimeout(() => heart.remove(), 6000);
}, 300);
</script>

</body>
</html>
